# HushType 0.5.18 Preview 14

- 识别历史改为原生复用列表，完整显示多行文字；调宽时以顶部记录为固定点，平滑调整其他行，避免在历史中段大幅跳动。
- 恢复侧栏展开、收起时的动态宽度过渡，修复侧栏边缘光标与导航按钮拖动冲突。
- 顶部模糊随滚动速度连续、平滑变化，减少滚动时重复布局检查。
- Preview 新增“调试”页，可开关自动模糊、调整半径范围、恢复默认和打开系统日志；配置即时生效。正式版隐藏调试入口。

已知限制：顶栏靠近下边缘的部分空白位置仍不能拖动窗口，精确扩展范围继续暂缓。启动顶栏延迟仍待后续定位。

本版保留现有历史数据、搜索、筛选、编号和删除确认，不改录音、模型或权限配置。
