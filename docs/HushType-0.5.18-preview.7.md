# HushType 0.5.18 Preview 7

- 修复 Preview 6 选择 iPhone、AirPods 等无线输入后仍会因蓝牙隐私声明缺失而闪退的问题。
- 改用 Apple 公开的蓝牙控制器状态接口，并添加蓝牙用途说明；首次使用无线输入时由 macOS 请求一次授权。
- 蓝牙权限仅用于判断所选无线麦克风是否断开；拒绝授权不会影响内置或有线麦克风，断开判断会退回现有音频健康检查。

顶栏图标只有在 HushType 进程存活时才会存在。Thaw 当前仍将 HushType 记录在“隐藏”区域；应用稳定运行后可在 Thaw 中将它移到“显示”区域。
