# HushType 0.5.18 Preview 10

- 按确认的 HTML 原型移植原生侧栏动画：收起时按钮跟随面板，到红绿灯右侧限位停下；展开时面板先追上按钮，再一起移动。
- 侧栏、按钮和内容区共用一个布局进度，全程保留同一个按钮，移除产生重复按钮的 NavigationSplitView 容器。
- 保留系统红绿灯和原生玻璃侧栏，不嵌入网页。
- 保留历史页分组与编号缓存，以及移除宽度回写状态的性能优化。
- 不改录音、设备断开或权限逻辑。

请验收原生窗口的按钮轨迹、顶部对齐，以及历史页侧栏展开／收起的流畅度。
