# HushType 0.5.18 Preview 12

- 历史页展开、收起侧栏时，文字按目标宽度一次排版，避免动画中反复换行和条目跳动；保留按钮动画和其他界面样式。
- 更新重启后打开设置；开机自启仍只驻留菜单栏。
- 通用更新区域新增“更新后静默启动”开关，默认关闭；开启后更新重启不弹设置。
- 不改录音、输入设备或权限逻辑。

更新启动行为需要由含本次改动的版本发起：安装 Preview 12 后的下一次更新可验收；旧版本更新到 Preview 12 尚不会写入新的重启标记。

动画效果交由用户手动验收。
