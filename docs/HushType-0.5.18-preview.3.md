# HushType 0.5.18 Preview 3

本预览版本根据 Preview 2 的真机反馈修正输入断开检测和加载圈视觉。

- 加载圈改为彼此分离的细圆条，保留已确认的偏右位置。
- 修复使用 iPhone 麦克风录音时，从 Mac 控制中心关闭蓝牙可能仍停留在“监听中”的问题。
- 当前采集设备连续两次从 CoreAudio 消失或变为不可用后，统一显示“连接断开”；短暂设备列表刷新不会误报。
- 设备存活检查只在收到首个有效音频后启用，不干扰 iPhone 的启动连接和既有自动回退边界。

加载圈视觉及 Mac 端关闭蓝牙后的断开状态仍需真机验收。
